from board import RED, BLACK, BLUE, GREY, SQUARE_SIZE
import pygame


class Piece:
    PADDING = 15
    OUTLINE = 2

    def __init__(self, row, col, color):
        self.row = row
        self.col = col
        self.color = color
        self.king = False
        self.x = 0
        self.y = 0
        self.calc_pos()

    def calc_pos(self):
        self.x = SQUARE_SIZE * self.col + SQUARE_SIZE // 2
        self.y = SQUARE_SIZE * self.row + SQUARE_SIZE // 2

    def make_king(self):
        self.king = True

    def draw(self, win):
        radius = SQUARE_SIZE // 2 - self.PADDING
        pygame.draw.circle(win, GREY, (self.x, self.y), radius, self.OUTLINE)
        pygame .draw.circle(win, self.color, (self.x, self.y), radius)

    def row(self, row, col):
        self.row = row
        self.col = col
        self.calc_pos()
#Function to get actions for player(state), takes two arguments current board state and current player 
    def get_Actions(state, player):
        actions = []
        for i in range(board_Size):
            for a in range(board_Size):
                if state[i, a] == player:
                    if 0 <= x < board_Size and 0 <= y < board_Size and state[x,y] == 0:
                            actions.append((i,a,x,y))
        return actions
        
